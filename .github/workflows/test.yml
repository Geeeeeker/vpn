name: VPN-Ultimate-Deploy

on:
  workflow_dispatch:
  repository_dispatch:
    types: [start_proxy]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Install Xray and Cloudflared
        run: |
          # 1. 安装 Xray
          sudo bash -s -- install < <(curl -L https://github.com/XTLS/Xray-install/raw/main/install-release.sh)
          # 2. 安装 Cloudflared
          curl -L -o cf.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cf.deb

      - name: Configure and Start Xray
        run: |
          # 停止默认服务，防止端口 8080 被占用
          sudo systemctl stop xray || true
          # 写入你的专用配置 (UUID 和 WebSocket 路径)
          cat <<EOF | sudo tee /usr/local/etc/xray/config.json
          {
              "inbounds": [{
                  "port": 8080,
                  "protocol": "vless",
                  "settings": {
                      "clients": [{"id": "d342d11e-d424-4583-b36e-52490062b323"}],
                      "decryption": "none"
                  },
                  "streamSettings": {
                      "network": "ws",
                      "wsSettings": {"path": "/speedtest"}
                  }
              }],
              "outbounds": [{"protocol": "freedom"}]
          }
          EOF
          # 启动 Xray 进程
          sudo /usr/local/bin/xray run -c /usr/local/etc/xray/config.json &

      - name: Run Cloudflare Tunnel
        env:
          TUNNEL_TOKEN: ${{ secrets.CF_TUNNEL_TOKEN }}
        run: |
          # 启动隧道
          cloudflared tunnel run --token $TUNNEL_TOKEN
